{
  "name": "command-middleware",
  "version": "0.0.1",
  "description": "Offers a Socket.IO interface to Pulurobotics robot.",
  "main": "main.js",
  "scripts": {
    "start": "forever start main.js",
    "stop": "forever stop main.js",
    "restart": "forever restart main.js",
    "watch": "nodemon main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "git commit -a -m 'build' && git push"
  },
  "author": "Fabian Fagerholm",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^1.18.7"
  },
  "dependencies": {
    "express": "^4.16.4",
    "socket.io": "^2.2.0"
  }
}
